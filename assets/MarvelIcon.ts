import { createIconSetFromIcoMoon } from 'react-native-vector-icons';

import marvelIconConfig from './marvel.json';

/* eslint-disable no-undef */
export default createIconSetFromIcoMoon(marvelIconConfig);
